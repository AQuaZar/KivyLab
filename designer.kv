MainMenu:

<MainMenu>:
    canvas:
        Color:
            rgba: 0.5, 0.5, 0.5, 0.5
        Rectangle: #woohoo!!!
            size: self.size
            pos: self.pos

<AppGraph>:
    canvas:
        Color:
            rgba: 0.3, 0.3, 0.3, 0.3
        Rectangle: #woohoo!!!
            size: self.size
            pos: self.pos
#<NodeLabel>:
#    canvas.before:
#        Color:
#            rgba: 0.5, 0.5, 0.5, 0.5
#        Ellipse:
#            size: min(self.size)*0.4, min(self.size)*0.4
#            pos: (self.x + self.width/2) - min(self.size)*0.4/2, (self.y + self.height/2 - min(self.size)*0.4/2)

